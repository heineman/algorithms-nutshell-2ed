To launch all unit tests, execute the following command:

PYTHON -m unittest

where PYTHON is your preferred way to launch the Python interpreter

If you want some more information about the test cases as they run, then:

PYTHON -m unittest -v
